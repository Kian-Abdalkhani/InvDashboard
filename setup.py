from setuptools import setup, find_packages  # Assuming setuptools is installed (pip install setuptools)

setup(
  name='inv_dashboard',  # Replace with your project name
  version='0.1.0',  # Replace with your version number
  packages=find_packages(),  # Automatically find packages
)